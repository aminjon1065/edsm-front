import React from 'react';
import usePageTitle from "../../hooks/usePageTitle";

const Index = () => {
    usePageTitle("Файлы")
    return (
        <div>
            Files
        </div>
    );
};

export default Index;