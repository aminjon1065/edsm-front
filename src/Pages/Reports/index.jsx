import React from 'react';
import usePageTitle from "../../hooks/usePageTitle";

const Index = () => {
    usePageTitle("Отчёты")
    return (
        <div>
            Report
        </div>
    );
};

export default Index;